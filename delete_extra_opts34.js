var items = document.querySelectorAll('.comp-edit-question'); var q = items[33]; function deleteLastOpt(){ var delBtns = q.querySelectorAll('.btn-delete-option, [class*="delete"]'); var opts = q.querySelectorAll('[contenteditable=true]'); if(opts.length > 11){ var lastDelBtn = delBtns[delBtns.length-1]; if(lastDelBtn){ lastDelBtn.click(); return true; } } return false; } var count = 0; function loop(){ if(deleteLastOpt()){ count++; setTimeout(loop, 300); } else { window._deleteCount = count; } } loop(); 'deleting extras...';