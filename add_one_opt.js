var items = document.querySelectorAll('.comp-edit-question'); var q = items[32]; var addLinks = q.querySelectorAll('a'); var addBtn = null; addLinks.forEach(function(el){ if(el.textContent.trim() === '添加选项'){ addBtn = el; } }); if(addBtn){ addBtn.click(); setTimeout(function(){ var opts = q.querySelectorAll('[contenteditable=true]'); var last = opts[opts.length-1]; last.focus(); document.execCommand('selectAll', false, null); document.execCommand('insertText', false, '11 packs - 1100 rolls'); }, 300); '添加选项 clicked'; } else { 'not found'; }