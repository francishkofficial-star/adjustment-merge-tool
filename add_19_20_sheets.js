var items = document.querySelectorAll('.comp-edit-question'); var q = items[33]; var remaining = ['19 sheets','20 sheets']; function addNext(idx){ if(idx >= remaining.length) return; var addLinks = q.querySelectorAll('a'); var addBtn = null; addLinks.forEach(function(el){ if(el.textContent.trim() === '添加选项'){ addBtn = el; } }); if(!addBtn) return; addBtn.click(); setTimeout(function(){ var opts = q.querySelectorAll('[contenteditable=true]'); var last = opts[opts.length-1]; last.focus(); document.execCommand('selectAll', false, null); document.execCommand('insertText', false, remaining[idx]); setTimeout(function(){ addNext(idx+1); }, 400); }, 400); } addNext(0); 'adding 19 and 20 sheets';