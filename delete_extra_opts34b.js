var items = document.querySelectorAll('.comp-edit-question'); var q = items[33]; function deleteLastOpt(){ var delBtns = q.querySelectorAll('button.btn-delete-option'); var opts = q.querySelectorAll('[contenteditable=true]'); if(opts.length > 11 && delBtns.length > 0){ delBtns[delBtns.length-1].click(); return true; } return false; } var count = 0; function loop(){ if(deleteLastOpt()){ count++; setTimeout(loop, 400); } else { window._deleteCount = count; } } loop(); 'deleting...';